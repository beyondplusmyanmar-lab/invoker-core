node_modules/
bun.lockb
*.log

# local workspace state (never committed; per-user, may contain artifacts)
.workspace/
*.sqlite
*.sqlite-*

# pilot snapshots from scripts/pilot-collect (per-run; contains a support bundle)
pilot/
# stray support bundles from `invoker support bundle` (per-run; redacted but local)
support-*.zip

# resolved secrets must never land in the tree
*.key
secrets.local.*
